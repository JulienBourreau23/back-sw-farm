nelmio_api_doc:
    documentation:
        info:
            title: SW Farm API
            description: API backend Summoners War Farm Tracker
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/(?!_)
